[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    db: integration tests that need a reachable MySQL server (deselect with -m "not db")
filterwarnings =
    # "DROP TABLE IF EXISTS" on an absent table warns; that's the point of IF EXISTS.
    ignore:Unknown table.*:Warning
